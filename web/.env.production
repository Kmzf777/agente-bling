# Lido pelo Vite no build de produção (Vercel). Aponta o frontend para o backend
# local exposto via ngrok. É uma URL pública (vai no bundle), então pode ser versionada.
VITE_API_BASE=https://basis-sleeve-baked.ngrok-free.dev
